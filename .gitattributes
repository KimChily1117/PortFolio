# Ignore rules belong in .gitignore, not .gitattributes.
# Unreal asset LFS rules are maintained in:
# Unreal Project/Lyra_Clone/LyraClone/.gitattributes
# Keep those rules so Content and plugin assets work across PCs.
