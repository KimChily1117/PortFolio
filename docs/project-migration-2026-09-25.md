# 작업 프로젝트 이전 기록

2026-09-25에 `E:\task`의 D3D와 Kimchily 두 작업 묶음을 이 저장소에 반영했습니다. 원본 폴더는 보존하고, 저장소에만 있던 파일도 삭제하지 않았습니다. 덮어쓴 파일은 이 작업을 수행한 로컬 작업공간에 별도로 백업했습니다.

| 작업 묶음 | 저장소 경로 | 새 경로에서의 준비 |
|---|---|---|
| DirectX 클라이언트와 C# 서버 | `C++/D3D_Popol`, `C++/D3D_Server` | [빌드·검증 안내](../C++/D3D_Popol/MIGRATION.md) |
| Unity WebGL·Expo·월드 게시 서버 | `KimchilyWebGL` | [설치·실행 안내](../KimchilyWebGL/MIGRATION.md) |

소스, 설정, 필요한 에셋·외부 라이브러리, 테스트와 개발 문서를 이전했습니다. 새로 생성된 캐시·중간 빌드 파일·로컬 인증 설정은 커밋 범위에서 제외합니다. 기존 저장소에서 이미 추적하던 생성물은 이번에 일괄 삭제하거나 추적 해제하지 않습니다.

Kimchily의 게시된 월드·인증 토큰·개발 서버 상태와 WebGL 플레이어 빌드는 소스와 별도로 취급합니다. 소스를 받은 뒤 실행하려면 프로젝트별 안내에 따라 의존성을 준비하고 필요한 산출물을 빌드해야 합니다.

LyraClone의 이번 작업 변경분은 GitHub LFS 예산 문제로 업로드 범위에서 제외했습니다. 기존 저장소의 Lyra 파일과 LFS 규칙은 유지하며, 새 변경분은 원본 작업 폴더와 로컬 백업 브랜치에 보존했습니다.

`Unity_Project`의 Unity 2022/Android 보존본, `LyraStarterGame`, 별도의 Project Dawn 작업본과 ProjectTwin은 이번 이전 범위에 포함하지 않았습니다.

## 검증 결과

- D3D: C++ 클라이언트·라이브러리 및 C# 서버 Debug 빌드 성공. C++ NavGrid 279개와 C# NavGrid 436개 검사를 통과했고 결과 JSON이 일치했습니다. Annie Q 직렬화·변환, Annie W 범위 12개·변환, CombatMovement 86개 검사도 통과했습니다.
- 기존 NavGrid 테스트의 정상 플레이어 상태를 현재 서버 전제조건에 맞췄습니다. 테스트 3개 파일에서 HP 초기화 2곳과 Annie/Garen 챔피언 초기화 2곳만 보정했으며 운영 코드와 기존 assertion은 유지했습니다.
- Kimchily: WebApp 59개, Expo 28개, WebGL host 25개, Python 보존 검사 8개로 총 120개 테스트 통과. Unity/Android 빌드·휴대전화 시연·EAS 배포는 실행하지 않았습니다.
- 원본 보존, 복사 파일 해시 및 커밋 대상의 캐시·로컬 인증 설정 제외를 확인했습니다. 검증용 빌드 결과는 별도 보관하고 커밋에서는 제외했습니다.
